---
name: Seema
on:
  push:
    branches:
      - main
jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - name: say hello to users
        run: echo "Hello Dosto"
      - name: say hello to username
        run: echo "Hello $USER"
  seema:
    runs-on: ubuntu-latest
    steps:
      -name: show what is the version of docker
      run: docker --version
